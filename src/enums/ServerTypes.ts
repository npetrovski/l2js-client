export enum ServerTypes {
  SERVER_NORMAL = 0x01,
  SERVER_RELAX = 0x02,
  SERVER_TEST = 0x04,
  SERVER_NOLABEL = 0x08,
  SERVER_CREATION_RESTRICTED = 0x10,
  SERVER_EVENT = 0x20,
  SERVER_FREE = 0x40,
}
